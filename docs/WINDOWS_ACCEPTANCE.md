# Rain Windows 11 验收清单

更新时间：2026-07-14

本文只覆盖用户指定的 Windows 11 x64 版本。状态含义：

- **通过（自动）**：本仓库中的自动测试已经验证。
- **通过（冒烟）**：本机开发环境已实际启动并检查界面/进程行为。
- **待真实环境**：需要模型权重、第三方应用、发布凭据或干净机器，不能用模拟测试代替。

## 自动验证结果

| 范围 | 状态 | 证据 |
| --- | --- | --- |
| 状态机、三阶段取消、迟到结果丢弃 | 通过（自动） | `cargo test --all-targets --manifest-path .\src-tauri\Cargo.toml` |
| 空闲卸载计时器 | 通过（自动） | 同上 |
| 配置默认值、迁移、损坏备份和约束 | 通过（自动） | 同上；默认语言断言为 `zh-CN` |
| 模型清单、路径边界、续传计数、哈希失败 | 通过（自动） | 同上 |
| 模型清单更新后保留旧版本、显式删除旧版本 | 通过（自动） | 同上 |
| 推理组件自动选择、HTTPS/路径清单约束 | 通过（自动） | 同上；NVIDIA 存在时选 GPU，否则选 CPU |
| 剪贴板并发恢复决策、输入目标变化判断 | 通过（自动） | 同上 |
| Worker 握手、请求 ID、二进制 PCM | 通过（自动） | 同上 |
| Worker 超时、重复/迟到响应、正常关闭后强杀兜底 | 通过（自动） | 同上 |
| Worker 连续崩溃熔断 | 通过（自动） | 同上 |
| 三个模型适配器统一契约 | 通过（自动） | `python -m unittest worker.test_worker -v`，使用模拟模型 |
| 主界面、浮条、取消窗口脚本语法 | 通过（自动） | `node --check` 三个脚本 |
| 发布 PowerShell 语法 | 通过（自动） | PowerShell AST 解析 |
| Windows x64 发行模式核心编译 | 通过（自动） | `cargo build --release --manifest-path .\src-tauri\Cargo.toml` |
| 中文首次启动、设置页、模型页、语言切换、无更新配置提示 | 通过（冒烟） | Windows 11 开发版交互检查 |
| 无模型时主程序正常进入、托盘继续运行、Worker 可关闭 | 通过（冒烟） | Windows 11 开发版进程检查 |

## V1.0 Windows 验收场景

以下场景在正式发布前必须逐项实测。自动测试覆盖只能证明核心不变量，不能替代真实应用和真实模型矩阵。

| # | 场景 | 当前状态 |
| --- | --- | --- |
| 1 | 全新安装可跳过组件/模型下载并进入应用 | 开发版通过；干净机器安装待真实环境 |
| 2 | 下载任一模型后无需重启即可使用 | 待真实模型 |
| 3 | 按住模式和开关模式均能录音、识别、输入 | 待真实模型与目标应用 |
| 4 | 模型冷启动期间录音内容不丢失 | 状态机通过；待真实模型计时 |
| 5 | 原目标未变化时结果进入原输入框 | 目标判断通过；待应用矩阵 |
| 6 | 目标变化时只进入剪贴板且不抢焦点 | 判断逻辑通过；待应用矩阵 |
| 7 | 正常粘贴后恢复原剪贴板全部格式 | 决策逻辑通过；待剪贴板格式矩阵 |
| 8 | 等待期间用户复制新内容时不覆盖 | 通过（自动）；待桌面实测 |
| 9 | `Esc` 和取消按钮在录音、等待模型、识别阶段有效 | 通过（自动）；待真实模型 |
| 10 | 默认一分钟到时自动停止并识别 | 配置/计时实现；待桌面计时 |
| 11 | Worker 崩溃后主程序和托盘继续运行 | 熔断通过；待打包 Worker 实测 |
| 12 | GPU 失败后由用户确认回退 CPU | 流程实现；待 CUDA 环境 |
| 13 | 退出后无音频、识别历史或 Worker 残留 | 开发冒烟通过；待安装版 |
| 14 | 三个模型都通过统一适配器契约 | 模拟模型通过；真实模型待验证 |
| 15 | Windows 11 x64 主要应用矩阵通过 | 待真实环境 |

## Windows 输入应用矩阵

每一项均测试：按住模式、开关模式、目标不变、识别期间切换目标、取消、剪贴板恢复、Unicode 模拟输入。

- [ ] 记事本
- [ ] Microsoft Word
- [ ] Chrome
- [ ] Edge
- [ ] VS Code
- [ ] Electron 应用
- [ ] 管理员权限应用（验证 Windows 完整性级别限制与剪贴板回退）
- [ ] 无输入框的桌面环境
- [ ] 全屏应用中的浮条隐藏/显示设置

## 剪贴板专项矩阵

- [ ] 纯文本
- [ ] 图片
- [ ] 文件列表
- [ ] 同时存在多个剪贴板格式
- [ ] 等待识别时用户主动复制新内容
- [ ] 剪贴板被其他程序占用
- [ ] 目标窗口在识别期间关闭
- [ ] 粘贴成功但恢复失败

## 设备与模型矩阵

- [ ] SenseVoice Small：CPU 加载、空音频、短音频、中文/英文、取消、卸载
- [ ] Fun-ASR Nano：CPU 加载、短音频、中文/英文、取消、卸载
- [ ] Paraformer-zh：CPU 加载、短音频、中文、取消、卸载
- [ ] 受支持的 NVIDIA CUDA 环境：自动选择、显存不足、用户确认回退 CPU
- [ ] 无 CUDA 的纯 CPU 机器
- [ ] 首次配置只下载匹配的 CPU 或 NVIDIA 推理组件，断点续传后可直接启用
- [ ] 麦克风启动失败
- [ ] 录音中拔出麦克风
- [ ] 默认 60 秒自动停止和自定义上限

## 隐私与发布验收

- [ ] 安装目录和应用数据目录不存在录音文件
- [ ] 日志、诊断包和崩溃报告中不存在识别文本、剪贴板内容、窗口标题和音频
- [ ] 默认状态下没有遥测或崩溃上传
- [ ] 模型下载仅在用户明确操作后发生
- [ ] 推理组件下载仅在用户明确操作后发生，且 SHA-256 失败时不启用
- [ ] 应用更新、模型下载与推理组件下载使用不同端点和流程
- [ ] 模型删除无法越过应用管理的模型根目录
- [ ] CPU/NVIDIA PyInstaller Worker 分别在无 Python 的干净 Windows 11 x64 虚拟机启动
- [ ] 基础 NSIS 不包含 PyTorch/CUDA，首次安装后可按硬件下载正确组件
- [ ] NSIS 安装、升级、卸载和开机启动行为正确
- [ ] GitHub Releases 更新清单与产物签名验证成功
- [ ] 用户拒绝更新时不会下载或安装
- [ ] 用户确认更新后才下载、安装和重启

## 性能记录

正式验收时分别记录，不把模型加载/推理时间混入应用处理时间：

- 快捷键触发到开始录音：目标不超过 100 ms。
- 浮条出现：目标不超过 150 ms。
- 已加载模型时停止录音到发送音频：目标不超过 100 ms。
- Worker 返回到开始注入：目标不超过 100 ms。
- 空闲常驻 CPU 接近 0%，未加载模型时不占用 GPU 显存。
